server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
    }

    location / {
        proxy_pass http://nivo-app/;
        proxy_set_header Host $host;
    }
}
