services:
  backend:
    build: ./backend
    environment:
      CLICKHOUSE_HOST: clickhouse
      CLICKHOUSE_PORT: 9000
      CLICKHOUSE_USER: analytics
      CLICKHOUSE_PASSWORD: analytics
      CLICKHOUSE_DATABASE: analytics
    networks: [lab]

  d3-app:
    build: ./apps/d3
    depends_on: [backend]
    networks: [lab]

  master:
    image: nginx:1.25-alpine
    depends_on:
      - backend
      - d3-app
    volumes:
      - ./master-nginx/single/d3.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - 8102:80
    networks: [lab]

networks:
  lab:
    external: true
    name: learning-lab_docker_core_lab
