apiVersion: v1
kind: Service
metadata:
  name: {{ include "uvdesk.fullname" . }}
  labels:
    {{- include "uvdesk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "uvdesk.selectorLabels" . | nindent 4 }}
