apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kerberos-io.fullname" . }}
  labels:
    {{- include "kerberos-io.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "kerberos-io.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kerberos-io.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
