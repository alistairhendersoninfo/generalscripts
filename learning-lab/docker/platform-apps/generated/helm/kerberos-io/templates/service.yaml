apiVersion: v1
kind: Service
metadata:
  name: {{ include "kerberos-io.fullname" . }}
  labels:
    {{- include "kerberos-io.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "kerberos-io.selectorLabels" . | nindent 4 }}
