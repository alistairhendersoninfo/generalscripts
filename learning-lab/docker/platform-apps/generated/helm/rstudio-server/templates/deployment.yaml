apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rstudio-server.fullname" . }}
  labels:
    {{- include "rstudio-server.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "rstudio-server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rstudio-server.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
