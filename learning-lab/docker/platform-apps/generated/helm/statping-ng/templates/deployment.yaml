apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "statping-ng.fullname" . }}
  labels:
    {{- include "statping-ng.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "statping-ng.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "statping-ng.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
