apiVersion: v1
kind: Service
metadata:
  name: {{ include "statping-ng.fullname" . }}
  labels:
    {{- include "statping-ng.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "statping-ng.selectorLabels" . | nindent 4 }}
