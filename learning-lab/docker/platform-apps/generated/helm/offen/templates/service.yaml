apiVersion: v1
kind: Service
metadata:
  name: {{ include "offen.fullname" . }}
  labels:
    {{- include "offen.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "offen.selectorLabels" . | nindent 4 }}
