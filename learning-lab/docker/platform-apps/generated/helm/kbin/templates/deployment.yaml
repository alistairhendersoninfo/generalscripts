apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kbin.fullname" . }}
  labels:
    {{- include "kbin.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "kbin.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kbin.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
