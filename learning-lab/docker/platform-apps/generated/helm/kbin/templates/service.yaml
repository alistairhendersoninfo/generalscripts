apiVersion: v1
kind: Service
metadata:
  name: {{ include "kbin.fullname" . }}
  labels:
    {{- include "kbin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "kbin.selectorLabels" . | nindent 4 }}
