apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "linkace.fullname" . }}
  labels:
    {{- include "linkace.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "linkace.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "linkace.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
