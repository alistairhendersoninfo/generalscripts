apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "drone-ci.fullname" . }}
  labels:
    {{- include "drone-ci.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "drone-ci.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "drone-ci.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
