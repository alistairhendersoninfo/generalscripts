apiVersion: v1
kind: Service
metadata:
  name: {{ include "drone-ci.fullname" . }}
  labels:
    {{- include "drone-ci.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "drone-ci.selectorLabels" . | nindent 4 }}
