apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cvat.fullname" . }}
  labels:
    {{- include "cvat.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "cvat.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cvat.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
