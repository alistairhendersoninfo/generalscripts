apiVersion: v1
kind: Service
metadata:
  name: {{ include "cvat.fullname" . }}
  labels:
    {{- include "cvat.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "cvat.selectorLabels" . | nindent 4 }}
