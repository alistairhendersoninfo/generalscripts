apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-superset.fullname" . }}
  labels:
    {{- include "apache-superset.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "apache-superset.selectorLabels" . | nindent 4 }}
