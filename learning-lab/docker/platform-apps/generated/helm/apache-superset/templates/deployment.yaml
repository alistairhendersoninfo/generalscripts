apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "apache-superset.fullname" . }}
  labels:
    {{- include "apache-superset.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "apache-superset.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "apache-superset.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
