apiVersion: v1
kind: Service
metadata:
  name: {{ include "appwrite.fullname" . }}
  labels:
    {{- include "appwrite.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "appwrite.selectorLabels" . | nindent 4 }}
