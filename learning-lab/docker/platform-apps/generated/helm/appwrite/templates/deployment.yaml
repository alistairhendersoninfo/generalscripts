apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "appwrite.fullname" . }}
  labels:
    {{- include "appwrite.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "appwrite.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "appwrite.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
