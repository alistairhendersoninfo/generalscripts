apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "open-streaming-platform.fullname" . }}
  labels:
    {{- include "open-streaming-platform.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "open-streaming-platform.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "open-streaming-platform.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
