apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shaarli.fullname" . }}
  labels:
    {{- include "shaarli.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "shaarli.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "shaarli.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
