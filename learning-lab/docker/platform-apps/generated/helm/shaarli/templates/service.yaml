apiVersion: v1
kind: Service
metadata:
  name: {{ include "shaarli.fullname" . }}
  labels:
    {{- include "shaarli.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "shaarli.selectorLabels" . | nindent 4 }}
