apiVersion: v1
kind: Service
metadata:
  name: {{ include "docspell.fullname" . }}
  labels:
    {{- include "docspell.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "docspell.selectorLabels" . | nindent 4 }}
