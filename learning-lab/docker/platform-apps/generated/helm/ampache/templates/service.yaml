apiVersion: v1
kind: Service
metadata:
  name: {{ include "ampache.fullname" . }}
  labels:
    {{- include "ampache.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ampache.selectorLabels" . | nindent 4 }}
