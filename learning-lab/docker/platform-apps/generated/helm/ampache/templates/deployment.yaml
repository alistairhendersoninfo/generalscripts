apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ampache.fullname" . }}
  labels:
    {{- include "ampache.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "ampache.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ampache.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
