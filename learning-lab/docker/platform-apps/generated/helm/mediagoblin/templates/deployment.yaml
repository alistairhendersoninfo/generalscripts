apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mediagoblin.fullname" . }}
  labels:
    {{- include "mediagoblin.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "mediagoblin.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mediagoblin.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
