apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediagoblin.fullname" . }}
  labels:
    {{- include "mediagoblin.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "mediagoblin.selectorLabels" . | nindent 4 }}
