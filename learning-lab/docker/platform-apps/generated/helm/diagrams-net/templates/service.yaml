apiVersion: v1
kind: Service
metadata:
  name: {{ include "diagrams-net.fullname" . }}
  labels:
    {{- include "diagrams-net.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "diagrams-net.selectorLabels" . | nindent 4 }}
