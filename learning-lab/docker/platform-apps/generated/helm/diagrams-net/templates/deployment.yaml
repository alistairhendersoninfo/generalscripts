apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "diagrams-net.fullname" . }}
  labels:
    {{- include "diagrams-net.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "diagrams-net.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "diagrams-net.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
