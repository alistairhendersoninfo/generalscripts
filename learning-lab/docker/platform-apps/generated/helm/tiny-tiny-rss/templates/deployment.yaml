apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tiny-tiny-rss.fullname" . }}
  labels:
    {{- include "tiny-tiny-rss.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "tiny-tiny-rss.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tiny-tiny-rss.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
