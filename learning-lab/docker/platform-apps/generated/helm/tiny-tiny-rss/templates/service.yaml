apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiny-tiny-rss.fullname" . }}
  labels:
    {{- include "tiny-tiny-rss.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tiny-tiny-rss.selectorLabels" . | nindent 4 }}
