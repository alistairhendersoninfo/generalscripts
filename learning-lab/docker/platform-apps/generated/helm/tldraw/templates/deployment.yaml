apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tldraw.fullname" . }}
  labels:
    {{- include "tldraw.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "tldraw.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tldraw.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
