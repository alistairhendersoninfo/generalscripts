apiVersion: v1
kind: Service
metadata:
  name: {{ include "tldraw.fullname" . }}
  labels:
    {{- include "tldraw.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tldraw.selectorLabels" . | nindent 4 }}
