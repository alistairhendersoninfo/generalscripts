apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "danswer.fullname" . }}
  labels:
    {{- include "danswer.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "danswer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "danswer.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
