apiVersion: v1
kind: Service
metadata:
  name: {{ include "danswer.fullname" . }}
  labels:
    {{- include "danswer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "danswer.selectorLabels" . | nindent 4 }}
