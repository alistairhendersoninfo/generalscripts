apiVersion: v1
kind: Service
metadata:
  name: {{ include "tryton.fullname" . }}
  labels:
    {{- include "tryton.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tryton.selectorLabels" . | nindent 4 }}
