apiVersion: v1
kind: Service
metadata:
  name: {{ include "gravitee-api-management.fullname" . }}
  labels:
    {{- include "gravitee-api-management.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "gravitee-api-management.selectorLabels" . | nindent 4 }}
