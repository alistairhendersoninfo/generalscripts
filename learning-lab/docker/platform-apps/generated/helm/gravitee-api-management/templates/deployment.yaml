apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gravitee-api-management.fullname" . }}
  labels:
    {{- include "gravitee-api-management.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "gravitee-api-management.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gravitee-api-management.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
