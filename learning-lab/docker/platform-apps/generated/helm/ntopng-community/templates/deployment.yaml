apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ntopng-community.fullname" . }}
  labels:
    {{- include "ntopng-community.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "ntopng-community.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ntopng-community.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
