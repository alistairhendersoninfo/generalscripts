apiVersion: v1
kind: Service
metadata:
  name: {{ include "ntopng-community.fullname" . }}
  labels:
    {{- include "ntopng-community.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ntopng-community.selectorLabels" . | nindent 4 }}
