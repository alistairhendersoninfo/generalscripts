apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "reportserver.fullname" . }}
  labels:
    {{- include "reportserver.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "reportserver.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "reportserver.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
