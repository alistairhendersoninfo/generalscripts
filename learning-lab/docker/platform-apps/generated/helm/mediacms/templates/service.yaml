apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediacms.fullname" . }}
  labels:
    {{- include "mediacms.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "mediacms.selectorLabels" . | nindent 4 }}
