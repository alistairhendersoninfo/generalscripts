apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mediacms.fullname" . }}
  labels:
    {{- include "mediacms.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "mediacms.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mediacms.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
