apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tube-archivist.fullname" . }}
  labels:
    {{- include "tube-archivist.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "tube-archivist.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tube-archivist.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
