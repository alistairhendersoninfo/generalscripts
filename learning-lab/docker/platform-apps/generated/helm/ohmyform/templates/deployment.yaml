apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ohmyform.fullname" . }}
  labels:
    {{- include "ohmyform.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "ohmyform.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ohmyform.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
