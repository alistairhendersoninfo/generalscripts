apiVersion: v1
kind: Service
metadata:
  name: {{ include "ohmyform.fullname" . }}
  labels:
    {{- include "ohmyform.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ohmyform.selectorLabels" . | nindent 4 }}
