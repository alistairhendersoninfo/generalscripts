apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookwyrm.fullname" . }}
  labels:
    {{- include "bookwyrm.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "bookwyrm.selectorLabels" . | nindent 4 }}
