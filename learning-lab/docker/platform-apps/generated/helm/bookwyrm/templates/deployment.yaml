apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bookwyrm.fullname" . }}
  labels:
    {{- include "bookwyrm.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "bookwyrm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bookwyrm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
