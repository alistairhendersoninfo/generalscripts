apiVersion: v1
kind: Service
metadata:
  name: {{ include "erpnext.fullname" . }}
  labels:
    {{- include "erpnext.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "erpnext.selectorLabels" . | nindent 4 }}
