apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "odoo-community.fullname" . }}
  labels:
    {{- include "odoo-community.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "odoo-community.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "odoo-community.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
