apiVersion: v1
kind: Service
metadata:
  name: {{ include "ackee.fullname" . }}
  labels:
    {{- include "ackee.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ackee.selectorLabels" . | nindent 4 }}
