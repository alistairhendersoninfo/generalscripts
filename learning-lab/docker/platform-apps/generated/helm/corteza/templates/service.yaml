apiVersion: v1
kind: Service
metadata:
  name: {{ include "corteza.fullname" . }}
  labels:
    {{- include "corteza.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "corteza.selectorLabels" . | nindent 4 }}
