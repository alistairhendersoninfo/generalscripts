apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "corteza.fullname" . }}
  labels:
    {{- include "corteza.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "corteza.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "corteza.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
