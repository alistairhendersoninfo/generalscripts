apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "apiman.fullname" . }}
  labels:
    {{- include "apiman.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "apiman.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "apiman.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
