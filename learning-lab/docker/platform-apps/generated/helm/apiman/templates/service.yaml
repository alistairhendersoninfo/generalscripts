apiVersion: v1
kind: Service
metadata:
  name: {{ include "apiman.fullname" . }}
  labels:
    {{- include "apiman.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "apiman.selectorLabels" . | nindent 4 }}
