apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "keystonejs.fullname" . }}
  labels:
    {{- include "keystonejs.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "keystonejs.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "keystonejs.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
