apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sonatype-nexus-oss.fullname" . }}
  labels:
    {{- include "sonatype-nexus-oss.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "sonatype-nexus-oss.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "sonatype-nexus-oss.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
