apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flarum.fullname" . }}
  labels:
    {{- include "flarum.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "flarum.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "flarum.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
