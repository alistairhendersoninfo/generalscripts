apiVersion: v1
kind: Service
metadata:
  name: {{ include "pimcore.fullname" . }}
  labels:
    {{- include "pimcore.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "pimcore.selectorLabels" . | nindent 4 }}
