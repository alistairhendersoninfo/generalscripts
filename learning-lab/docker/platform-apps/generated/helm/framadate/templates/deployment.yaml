apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "framadate.fullname" . }}
  labels:
    {{- include "framadate.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "framadate.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "framadate.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
