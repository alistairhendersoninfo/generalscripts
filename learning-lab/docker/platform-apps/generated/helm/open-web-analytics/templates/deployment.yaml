apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "open-web-analytics.fullname" . }}
  labels:
    {{- include "open-web-analytics.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "open-web-analytics.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "open-web-analytics.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
