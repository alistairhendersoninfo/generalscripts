apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-web-analytics.fullname" . }}
  labels:
    {{- include "open-web-analytics.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "open-web-analytics.selectorLabels" . | nindent 4 }}
