apiVersion: v1
kind: Service
metadata:
  name: {{ include "librephotos.fullname" . }}
  labels:
    {{- include "librephotos.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "librephotos.selectorLabels" . | nindent 4 }}
