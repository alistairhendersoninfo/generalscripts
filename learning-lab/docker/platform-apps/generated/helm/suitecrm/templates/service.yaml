apiVersion: v1
kind: Service
metadata:
  name: {{ include "suitecrm.fullname" . }}
  labels:
    {{- include "suitecrm.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "suitecrm.selectorLabels" . | nindent 4 }}
