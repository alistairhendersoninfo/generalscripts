apiVersion: v1
kind: Service
metadata:
  name: {{ include "parse-platform.fullname" . }}
  labels:
    {{- include "parse-platform.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "parse-platform.selectorLabels" . | nindent 4 }}
