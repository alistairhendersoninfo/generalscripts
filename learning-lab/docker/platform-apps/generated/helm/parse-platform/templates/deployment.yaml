apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "parse-platform.fullname" . }}
  labels:
    {{- include "parse-platform.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "parse-platform.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "parse-platform.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
