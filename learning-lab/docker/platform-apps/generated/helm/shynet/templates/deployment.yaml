apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shynet.fullname" . }}
  labels:
    {{- include "shynet.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "shynet.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "shynet.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
