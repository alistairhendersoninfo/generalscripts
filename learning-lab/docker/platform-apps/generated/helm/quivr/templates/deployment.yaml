apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "quivr.fullname" . }}
  labels:
    {{- include "quivr.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "quivr.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "quivr.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: app
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.service.port }}
