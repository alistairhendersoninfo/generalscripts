apiVersion: v1
kind: Service
metadata:
  name: {{ include "quivr.fullname" . }}
  labels:
    {{- include "quivr.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "quivr.selectorLabels" . | nindent 4 }}
