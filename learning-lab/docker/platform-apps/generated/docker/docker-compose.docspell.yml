# Notes: Needs Postgres and Elasticsearch services.
services:
  docspell:
    image: eikek0/docspell-restserver:23.3.0
    container_name: docspell
    restart: unless-stopped
    ports:
      - "7880:7880"
    environment:
      # TODO: define environment variables
    networks:
      - lab

networks:
  lab:
    external: true
    name: learning-lab_lab
