# Notes: Mount /nexus-data for persistence.
services:
  sonatype-nexus-oss:
    image: sonatype/nexus3:3.68.1
    container_name: sonatype-nexus-oss
    restart: unless-stopped
    ports:
      - "8082:8082"
    environment:
      # TODO: define environment variables
    networks:
      - lab

networks:
  lab:
    external: true
    name: learning-lab_lab
